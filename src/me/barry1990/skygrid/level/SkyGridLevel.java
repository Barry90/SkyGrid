package me.barry1990.skygrid.level;


@Deprecated
public enum SkyGridLevel {
	
	
	/*END_LEVEL(SkyGridLevelEnd.class, 1),	
	STONE_LEVEL(SkyGridLevelStone.class, 2),	
	WOOD_LEVEL(SkyGridLevelWood.class, 3);	
	
	public final Class<? extends ISkyGridLevel> c;
	public final int id;
	
	private SkyGridLevel(Class<? extends ISkyGridLevel> c, int i) {
		this.c = c;
		this.id = i;
	}
	
	public static Class<? extends ISkyGridLevel> getClassFromID(int id) {
		int count = SkyGridLevel.class.getEnumConstants().length;
		for (int i = 0; i < count; i++) {
			if (id == SkyGridLevel.class.getEnumConstants()[i].id)
				return SkyGridLevel.class.getEnumConstants()[i].c;
		}
		throw new IllegalArgumentException("No level found for id : " + id);
	}*/
	

}
